global:
  storageClass: "standard" # minikube default Storage Class

usePassword: true
password: "password"

persistence:
  size: 1Gi

redis:
  startupProbe:
    initialDelaySeconds: 60
    enabled: true

cluster:
  nodes: 6
  replicas: 1
