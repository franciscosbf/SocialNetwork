global:
  storageClass: "standard" # kind's default Storage Class

usePassword: true
password: "password"

tls:
  enabled: true
  autoGenerated: true

persistence:
  size: 1Gi

redis:
  startupProbe:
    enabled: true

cluster:
  nodes: 6
  replicas: 1
